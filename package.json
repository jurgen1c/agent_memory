{
  "name": "@jurgen1c/agent-memory-cli",
  "version": "0.2.0",
  "description": "Repository-local agent memory CLI based on atomic claims and generated indexes.",
  "license": "MIT",
  "main": "dist/agent-memory.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/jurgen1c/agent_memory"
  },
  "keywords": [
    "agent",
    "memory",
    "cli",
    "codex",
    "repository"
  ],
  "type": "module",
  "bin": {
    "agent-memory": "dist/agent-memory.js",
    "agentflow": "dist/agentflow.js"
  },
  "dependencies": {
    "@xyflow/react": "^12.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "eslint": "^10.5.0",
    "happy-dom": "^20.10.6",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.62.0",
    "vite": "^7.3.6"
  },
  "files": [
    "dist/",
    "packages/",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/run-root-verification.mjs build",
    "build:agent-memory": "bun build packages/cli/src/index.ts --target=node --outfile=dist/agent-memory.js",
    "build:agentflow": "bun build packages/agentflow-cli/src/index.ts --target=node --outfile=dist/agentflow.js",
    "build:agentflow-cli-package": "bun run --cwd packages/agentflow-cli build",
    "build:agent-tools": "bun run --cwd packages/agent-tools build",
    "build:web": "bun run --cwd packages/web build",
    "typecheck": "node scripts/run-root-verification.mjs typecheck",
    "typecheck:packages": "tsc --noEmit -p tsconfig.json",
    "typecheck:web": "tsc --noEmit -p packages/web/tsconfig.json",
    "audit": "bun audit --audit-level moderate",
    "ci": "bun run version:check && bun run audit && bun run lint && bun run typecheck && bun test --coverage && bun run build",
    "lint": "eslint .",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "version": "node scripts/sync-workspace-versions.mjs --stage",
    "version:check": "node scripts/sync-workspace-versions.mjs --check",
    "dev": "bun packages/cli/src/index.ts",
    "ui:mock": "scripts/run-mock-ui.sh",
    "prepack": "bun run build"
  },
  "engines": {
    "node": ">=25"
  }
}
